;(function($){
	$.fn.collapse = function(options) {
		var defaults = {
			url : '',
		}
		
		<ul></ul>
		
		var opts = $.extend({}, defaults, options||{});
		if (url.length>0) {
			$.ajax(ajaxOptions);
		}
		
		
		
		
	};
})(jQuery);